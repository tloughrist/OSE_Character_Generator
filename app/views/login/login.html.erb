<%= turbo_frame_tag "login" do %>
  <div class="card lg:card-side bg-base-100 shadow-xl mt-7">
    <figure><img
      src='/img/tramp_minotaur.jpg'
      alt="Might minotaur"
      class="h-80"
    /></figure>
    <div class="card-body">
      <%= form_with do |form| %>
        <div>
          <%= form.label :username, "Username" %>
          <%= form.text_field :username %>
        </div>
        <div>
          <%= form.label :password, "Password" %>
          <%= form.text_field :password %>
        </div>
        <%= form.submit "Submit" %>
      <% end %>
      <%= link_to "Create Account", welcome_signup_url %>
      <%= link_to "Forgot password?", welcome_forgotpassword_url %>
    </div>
  </div>
<% end %>